gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Except the development signing key. See dev-signing/README.md -- it is
# committed so that development builds install over each other, its password
# is in that file, and the release workflow refuses to use it.
!dev-signing/kyron-dev.jks
